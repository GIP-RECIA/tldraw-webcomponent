export type Settings = {
  roomId?: string;
  readOnly?: boolean;
  language?: string;
};

export type Default = {
  language: string;
};

export type Multiplayer = {
  roomId: string;
  language: string;
};
